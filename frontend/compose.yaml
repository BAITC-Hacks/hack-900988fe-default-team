services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-}
    image: hackalem-frontend:local
    ports:
      - "5380:80"
    restart: unless-stopped
