# Стартовый промпт для backend-агента

```text
Я nalrematvonesah.

Работаю над HackAlem AI в общей ветке main. Моя зона владения — только backend/** и docs/progress/nalrematvonesah.md.

Сначала полностью прочитай AGENTS.md, HACKATHON_PLAN.md, API_CONTRACT.md и docs/progress/nalrematvonesah.md. Затем изучи текущее состояние backend и сразу возьми первую незавершённую backend-задачу с максимальным приоритетом.

Работай автономно в рамках требований кейса. Не изменяй frontend/**, чужой progress-файл и общие файлы. Если необходимо изменение API-контракта или общего файла, запиши предложение в раздел «Предложения для интеграции» файла docs/progress/nalrematvonesah.md и продолжай совместимую реализацию, если это возможно.

После каждого значимого завершённого блока обновляй docs/progress/nalrematvonesah.md: что сделано, какие файлы изменены, какие проверки прошли, что делать дальше и какие есть блокеры. Не выполняй git commit, pull, rebase, checkout, reset или push: Git-операции выполняются отдельно и последовательно.

Не добавляй секреты и реальные API-ключи. Все переменные окружения документируй только через backend/.env.example. Поддерживай backend/README.md в соответствии с фактически работающим способом запуска.

Главный приоритет: детерминированный рейтинг, AI-вопросы без выдуманных фактов и работающий API по API_CONTRACT.md. Не трать время на функции вне must-have, пока основной сценарий не завершён и не проверен.
```

